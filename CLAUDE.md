## Agent skills

### Issue tracker

Ad-hoc — solo project, issues are reported directly in chat, no formal tracker. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context — `CONTEXT.md` + `docs/adr/` at the repo root (not created yet; skills create them lazily). See `docs/agents/domain.md`.
