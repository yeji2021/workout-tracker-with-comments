# 헬스 트래커 개발 TODO

> 2026-07-05 기획 조율 확정 스펙 기준. 위에서 아래 순서로 진행하면 각 단계마다 동작하는 결과물이 나오도록 구성.

## Phase 0 — 프로젝트 셋업
- [x] Vite + React + TypeScript 프로젝트 생성
- [x] Tailwind CSS 설정 (모바일 우선 레이아웃)
- [x] Supabase JS 클라이언트 연결 (`.env.local`의 publishable key 사용)
- [x] git 저장소 초기화 (`.env.local`은 `.gitignore`에 이미 등록됨)
- [x] 라우팅 뼈대: 홈(오늘 운동) / 내 루틴 / 통계 / 피드 4개 탭

## Phase 1 — Supabase 스키마 & 계정 구조
- [ ] 테이블 설계 및 생성 SQL 작성
  - `profiles` (id, 닉네임, 복구코드, 그룹 id)
  - `groups` (id, 초대코드)
  - `exercises` (운동 정의: 이름, 메인 타겟 부위, 보조 부위, 기본 제공 여부)
  - `workout_sessions` (날짜, 소유자, 공유 여부)
  - `workout_entries` (세션 내 운동 항목, 순서)
  - `sets` (무게, 횟수, 완료 여부)
  - `routines` / `routine_entries`
  - `reactions` (이모지), `comments`
- [ ] RLS(행 수준 보안) 정책: 개인 기록은 본인만, 공유된 세션은 같은 그룹만 읽기 — **친구 기록이 내 기록에 섞이지 않는 핵심 장치**
- [ ] 기본 운동 시드 데이터 (부위별 주요 운동 50~80개: 가슴/등/어깨/하체/팔/코어)
- [x] 온보딩 플로우: 닉네임 정하기 → 그룹 만들기 or 초대코드 입력
- [x] 복구 코드 발급/입력 (폰 변경 시 기록 복구)

## Phase 2 — 운동 기록 코어 (fleek 스타일)
- [~] 데일리 운동 시작: 자유 운동 (루틴 선택은 Phase 3)
- [x] 운동 추가: 저장된 운동 목록에서 선택 (메인 타겟 부위별 카테고리 탭)
- [x] 운동 검색 + 커스텀 운동 추가
- [x] 이전 운동 기록의 세트/무게로 default 자동 채움
- [x] 세트 추가/삭제/완료 체크
- [x] 운동 순서 변경 (플레이리스트처럼 드래그 앤 드롭)
- [x] 세트 완료 시 휴식 타이머 자동 시작 (시간 설정 가능, 백그라운드 대응)
- [x] "지난번보다 +2.5kg" 즉석 비교 표시
- [~] 세션 저장 완료 (과거 기록 날짜별 열람은 이후)

## Phase 3 — 루틴
- [x] 내 루틴 목록 화면
- [x] 루틴 추가: 운동 선택해서 구성
- [x] 기존 운동 기록 → 우측 상단 버튼 → 루틴으로 저장
- [x] 루틴으로 데일리 운동 시작하기
- [x] 루틴 수정/삭제/순서 변경

## Phase 4 — 통계
- [x] 부위별 주간 볼륨 계산 (무게×횟수 합산)
- [x] "이번주 [부위]를 [무게] 더/덜 들었어요" 요약 카드 + 부위별 바 그래프
- [x] 사용 근육 그래프: 신체 실루엣 히트맵, 기간 필터 7일/30일/90일/전체
- [x] 캘린더 히트맵 (운동한 날 잔디)
- [~] PR 목록 화면 완료 (기록 갱신 시 실시간 축하 표시는 이후)
- [x] AI 상담 프롬프트 생성: 통계치를 템플릿에 채워서 클립보드 복사 버튼

## Phase 5 — 소셜 (피드 + 이모지 + 댓글)
- [x] 세션 상세에서 "공유하기" → 그룹 피드에 게시 (opt-in)
- [x] 피드 화면: 그룹 멤버들의 최근 공유 운동 목록
- [x] 이모지 리액션 (setlog 스타일)
- [x] 댓글 작성/삭제, 댓글 스레드
- [x] Supabase Realtime으로 리액션/댓글 실시간 반영
- [~] PR 달성 뱃지 피드에 표시 (보류: 타인 전체기록 RLS 미노출로 공유시 PR 저장 필요)
- [x] 새 댓글/리액션 앱 내 뱃지 표시 (푸시 알림 아님)

## Phase 6 — PWA & 배포
- [ ] manifest.json + 아이콘 (홈 화면에 추가 대응)
- [ ] iOS 사파리 대응 점검 (safe area, 상태바, 스크롤 바운스)
- [ ] 서비스 워커: 정적 자원 캐싱 (앱 셸 오프라인 로드까지만 — 데이터 동기화는 2차)
- [ ] Vercel 배포 + 친구 공유용 링크 확정
- [ ] 데이터 내보내기 (JSON/CSV)

## 2차 (이번엔 안 함)
- 실시간 채팅
- 푸시 알림
- 오프라인 기록 동기화

---

## 메모
- Supabase 키는 `.env.local`에 있음. **publishable key만 클라이언트 코드에 사용**, secret key는 절대 프론트엔드에 넣지 않기 (스키마 생성 등 관리 작업에만 사용).
- 무료 티어는 1주일 미접속 시 일시정지 → 대시보드에서 재개 가능, 데이터 유지됨.
